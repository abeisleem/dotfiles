# Bind reloading tmux configuration file
unbind r
bind r source-file ~/.tmux.conf

# Start session & window numbering at 1
set -g base-index 1
setw -g pane-base-index 1

# Set prefix to Ctrl-s
unbind C-b
set -g prefix C-s
bind C-s send-prefix

# Enable mouse support
set -g mouse on
